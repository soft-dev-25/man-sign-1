name: Merge to Main

on:
  push:
    branches:
      - main
jobs:
  unit-tests:
    name: Run Unit Tests
    uses: ./.github/workflows/UnitTest.yml
  integration-db-tests:
    name: Run Integration DB Tests
    needs: unit-tests
    uses: ./.github/workflows/IntegrationDbTest.yml
